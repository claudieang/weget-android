<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/custom_dialog_layout_design_user_input"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="16dp">

    <TextView
        android:id="@+id/dialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reset Your Password"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/colorPrimary" />

    <TextView
        android:id="@+id/dialogSubText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Enter your email address to reset your password. An email will be sent with reset instructions."
        android:textSize="14sp"
        android:textColor="@color/darkGray" />

    <EditText
        android:id="@+id/userInputDialog"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:textColor="@color/colorPrimary"
        android:layout_below="@+id/dialogSubText"
        android:layout_marginTop="14dp"
        android:drawableLeft="@drawable/ic_email"
        android:paddingLeft="5dp"
        android:drawablePadding="5dp"
        android:hint="Enter email"
        android:background="@drawable/barbase"
        android:layout_centerHorizontal="true"
        android:inputType="text" />
</LinearLayout>